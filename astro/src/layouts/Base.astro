---
interface Props {
  title: string;
  description: string;
  canonicalPath?: string;
  ogImage?: string;
}

const { 
  title, 
  description, 
  canonicalPath = '', 
  ogImage = '/v2/i/logo-16x9.png' 
} = Astro.props;

const siteUrl = 'https://jrobertson-joiners.co.uk';
const canonicalUrl = `${siteUrl}/v2${canonicalPath}`;
const fullOgImage = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;

import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="en-GB">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <link rel="canonical" href={canonicalUrl} />

  <!-- Local SEO Geo Tags -->
  <meta name="geo.region" content="GB-STG" />
  <meta name="geo.placename" content="Stirling" />
  <meta name="geo.position" content="56.15516;-4.12703" />
  <meta name="ICBM" content="56.15516, -4.12703" />

  <!-- Block indexing until launch - REMOVE WHEN GOING LIVE -->
  <meta name="robots" content="noindex, nofollow" />
  
  <!-- Additional SEO meta tags -->
  <meta name="author" content="Jamie Robertson Joiners" />
  <meta name="publisher" content="Jamie Robertson Joiners" />
  <meta name="copyright" content="Jamie Robertson Joiners" />
  <meta name="format-detection" content="telephone=yes" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <meta name="apple-mobile-web-app-title" content="JR Joiners" />
  
  <meta name="theme-color" content="#1e3a8a" />
  <meta name="msapplication-TileColor" content="#1e3a8a" />
  <link rel="alternate" hreflang="en-GB" href={canonicalUrl} />

  <!-- Open Graph -->
  <meta property="og:locale" content="en_GB" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={canonicalUrl} />
  <meta property="og:image" content={fullOgImage} />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="675" />
  <meta property="og:image:alt" content="Jamie Robertson Joiners - Joinery and Building Contractor in Stirling" />
  <meta property="og:site_name" content="Jamie Robertson Joiners" />
  
  <title>{title}</title>
  
  <link rel="preconnect" href="https://formspree.io">
  <link rel="preload" href={`${import.meta.env.BASE_URL}fonts/Inter.woff2`} as="font" type="font/woff2" crossorigin>
  <link rel="apple-touch-icon" sizes="180x180" href={`${import.meta.env.BASE_URL}i/apple-touch-icon.png`}>
  <link rel="icon" type="image/png" sizes="192x192" href={`${import.meta.env.BASE_URL}i/android-chrome-192x192.png`}>
  <link rel="icon" href={`${import.meta.env.BASE_URL}i/favicon.ico`}>

  <slot name="head" />
</head>

<body class="bg-white text-slate-900 overflow-x-hidden">
  <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:top-3 focus:left-3 focus:z-50 focus:rounded-md focus:bg-white focus:px-4 focus:py-2 focus:text-slate-900">
    Skip to main content
  </a>
  
  <Header />
  
  <main id="main">
    <slot />
  </main>
  
  <Footer />

  <slot name="scripts" />
</body>
</html>
